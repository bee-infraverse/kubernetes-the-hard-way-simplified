{
  "extends": ["config:base"],
  "regexManagers": [
    {
      "fileMatch": ["^.*\\.sh$"],
      "matchStrings": [
        "^(?<var>[A-Z_]+)_VERSION=\\$\\{\\1:-v(?<currentValue>.+?)\\}.*?#\\s?renovate: datasource=(?<datasource>.*?) depName=(?<depName>.*?)$"
      ]
    }
  ]
}